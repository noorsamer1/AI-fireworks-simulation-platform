# Browser-only dev (`pnpm dev` in Chrome): point at a manually started backend.
# Start backend with the same port, e.g.:
#   set PYROMIND_SIDECAR_PORT=8765 && uv run python main.py
# VITE_SIDECAR_PORT=8765
